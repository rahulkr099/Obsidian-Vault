Alright Rahul, here comes a clean, interview-ready **Backend Machine Coding Round** mock question for a **Form Validation API**.  
This round looks simple, but interviewers use it to test **input validation, regex, error handling, reusable middleware, and API response design**.

I’ll keep it simple, friendly, and future-focused for you.

---

# 🧪 **Backend Machine Coding Round — Form Validation API**

### ⏱ Time Limit: 30–40 minutes

### 🎯 Goal

Build a backend API that validates:

- Email
    
- Phone number
    
- Password
    
- With clear error messages
    
- Clean and reusable validation logic (middleware or service)
    

---

# 📝 **Problem Statement**

Design an API endpoint that accepts user input in a form and validates:

- Email format
    
- Phone number format
    
- Password strength
    

If validation fails, return **specific and clear error messages**.

If all fields are valid, return a success response.

You may use:

- Node.js + Express
    
- Regex
    
- Custom validators
    
- JOI / Zod / Yup (optional, but writing custom logic is better for interviews)
    

---

# 🔧 **Required Endpoint**

```
POST /validate
```

### **Input Example**

```json
{
  "email": "rahul@gmail.com",
  "phone": "+919876543210",
  "password": "Rahul@123"
}
```

---

# 💡 **Validation Rules**

### **1️⃣ Email Validation**

Rules:

- Must match standard email pattern
    
- No spaces
    
- No invalid characters
    
- Should not allow partial emails like:
    
    - "abc@"
        
    - "@gmail.com"
        

Example regex:

```
/^[^\s@]+@[^\s@]+\.[^\s@]+$/
```

### **2️⃣ Phone Number Validation**

Rules:

- Must be a valid Indian or international format
    
- Must not contain letters
    
- Length check (10 digits for Indian numbers)
    
- Allowed formats:
    
    - `9876543210`
        
    - `+919876543210`
        
    - `+1-202-555-0134` (optional)
        

Example regex (basic):

```
/^\+?[0-9]{10,15}$/
```

### **3️⃣ Password Validation**

Rules:

- Minimum 8 characters
    
- Must contain:
    
    - 1 uppercase letter
        
    - 1 lowercase letter
        
    - 1 digit
        
    - 1 special character
        
- No spaces allowed
    

Example regex:

```
/^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@#$%^&+=!]).{8,}$/
```

---

# 🔁 **Response Requirements**

### **On Success**

```json
{
  "success": true,
  "message": "All fields are valid"
}
```

### **On Failure**

Return **field-specific errors**, example:

```json
{
  "success": false,
  "errors": {
    "email": "Invalid email format",
    "password": "Password must contain uppercase, lowercase, number, special character"
  }
}
```

Rules:

- Must show **only the fields that failed**
    
- Should not return success=true for partial validations
    
- Error messages should be clear and readable
    

---

# 🧪 **Edge Cases to Test**

- Missing fields → return `"email is required"`
    
- Empty strings → `"email cannot be empty"`
    
- Email with uppercase allowed
    
- Phone number with spaces → invalid
    
- Password without special character → error
    
- Very long input strings → safely handle (avoid ReDoS with regex)
    
- Invalid JSON structure → safe 400 response
    
- Trimming spaces:
    
    - `" email@gmail.com "` → valid after trimming
        
- SQL injection-like input → do not crash
    
- Unicode characters in password → allowed (if specs allow)
    

---

# 🧱 **Backend Design Requirements**

- Write **reusable validation functions**
    
- Or a **middleware** like:
    

```js
app.post("/validate", validateForm(), handler);
```

- Clean separation:
    
    - controller → reads request
        
    - validator → checks fields
        
    - service → not needed for this small task
        
- Avoid duplicating regex in multiple places
    
- Avoid nested if-else mess; use clean functional style
    

---

# ⭐ **Interviewer Follow-Up Questions**

1. Why is regex-only validation not enough?
    
2. How do you avoid **regex ReDoS attacks**?
    
3. How would you make validation reusable across multiple routes?
    
4. Should validation happen at:
    
    - controller level
        
    - middleware level
        
    - service layer?
        
5. How would you integrate **Joi/Zod** for schema validation?
    
6. How do you validate on both frontend + backend?
    
7. How would you internationalize phone validation?
    
8. Should passwords be trimmed or kept raw?
    
9. How would you extend this API for signup validation?
    

---

# 🌟 **Bonus Features (To Impress)**

- Add username validation
    
- Add OTP validation
    
- Add Aadhaar number / PAN number validation
    
- Add reusable validation library
    
- Add rate-limit to prevent brute force
    
- Add email domain whitelist/blacklist
    
- Add password strength scoring UI (optional)
    
- Add `/validate/field` endpoint for single-field validation
    

---

If you want, I can generate the next machine coding round:

➡️ **19. Blog Backend (CRUD + Tags + Search)**  
➡️ **20. Comment System (nested replies)**  
➡️ **21. Real-time Chat Backend**  
➡️ **22. Mini Social Media Backend (Posts + Likes + Feed)**

Just tell me the next one!