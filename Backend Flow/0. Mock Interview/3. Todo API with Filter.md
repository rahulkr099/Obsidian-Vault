Alright Rahul, here comes a clean, interview-grade **Backend Machine Coding Round** mock question for a **Todo API with Filters + Pagination**.  
This round is perfect for testing **query params, CRUD logic, filtering, clean architecture, and pagination** — all core backend fundamentals.

---

# ✅ **Backend Machine Coding Round — Todo API with Filters + Pagination**

### **⏱ Time Limit:** 60 minutes

### **🎯 Goal:** Build a full **Todo Management API** with CRUD operations, filtering, and pagination.

---

# **📝 Problem Statement**

Design and implement a **Todo REST API** with:

- Add todo
    
- Get all todos with **filter + pagination**
    
- Get todo by ID
    
- Update todo
    
- Delete todo
    

Use a 3-layer architecture:

- **Routes**
    
- **Controller**
    
- **Service**
    
- (Optional) **Repository**
    

The database may be in-memory, SQL, or MongoDB.

---

# **🧱 Todo Object Structure**

Each todo must have:

```json
{
  "id": "uuid or number",
  "title": "string",
  "completed": false,
  "createdAt": timestamp,
  "updatedAt": timestamp
}
```

---

# **🔧 Required Endpoints**

```
POST    /todos
GET     /todos
GET     /todos/:id
PUT     /todos/:id
DELETE  /todos/:id
```

---

# **💡 Functional Requirements**

### **1️⃣ Create Todo — POST /todos**

- Input: `{ title }`
    
- Rules:
    
    - Title is required
        
    - Title must be non-empty and trimmed
        
- New todos default to:
    
    - `completed: false`
        
    - `createdAt` & `updatedAt` timestamps
        
- Return created todo.
    

---

### **2️⃣ Get All Todos — GET /todos**

Support query parameters:

#### **a) Filter By Status**

```
/todos?status=active
/todos?status=completed
/todos?status=all   (default)
```

Rules:

- `active` → completed === false
    
- `completed` → completed === true
    
- unknown status → return 400
    

#### **b) Pagination**

```
/todos?page=1&limit=10
```

- Defaults: page = 1, limit = 10
    
- Negative or zero values → fallback to defaults
    
- Response must include:
    
    ```json
    {
      "page": 1,
      "limit": 10,
      "total": 42,
      "data": [ ...todos ]
    }
    ```
    

### **3️⃣ Get Todo by ID — GET /todos/:id**

- Return todo
    
- If not found → 404
    

---

### **4️⃣ Update Todo — PUT /todos/:id**

Accept updates for:

- `title`
    
- `completed`
    

Rules:

- Title cannot be empty
    
- Completed must be boolean
    
- Update `updatedAt`
    
- Return updated todo
    
- If todo not found → 404
    

---

### **5️⃣ Delete Todo — DELETE /todos/:id**

- Remove the todo
    
- If not found → 404
    
- Return success message
    

---

# **🧪 Edge Cases to Handle**

- Title is just spaces → invalid
    
- Status filter invalid → return 400
    
- Pagination:
    
    - page < 1 → default
        
    - limit < 1 → default
        
- Updating completed with string like "yes" → reject
    
- Tiny datasets:
    
    - No todos → page result = empty list
        
- Todo deletion then fetching → should return 404
    
- Keep IDs unique
    
- Large list (1000+) → pagination must not slow down
    
- Order: latest created first (optional but nice)
    

---

# **📦 Expected Responses**

### Successful list response:

```json
{
  "page": 1,
  "limit": 5,
  "total": 20,
  "data": [
    { "id": 1, "title": "Buy milk", "completed": false }
  ]
}
```

### Error response:

```json
{
  "error": true,
  "message": "Todo not found"
}
```

---

# ⭐ Interviewer Follow-Up Questions

1. How would you add sorting? (`sort=asc/desc`)
    
2. How would you implement soft-delete for todos?
    
3. How would you allow full-text search on title?
    
4. How would you handle pagination efficiently for 1M todos?
    
5. How would you secure the API with JWT authentication?
    
6. How would you write unit tests for service + controller?
    

---

# 🌟 Bonus Features (Great to Impress)

- Add **bulk delete** feature.
    
- Add **bulk update** (mark all completed).
    
- Add **search** filter: `/todos?search=milk`
    
- Add **priority** field (low/medium/high).
    
- Add **due dates** + filter by overdue.
    
- Add **tags** and tag-based filtering.
    
- Add **rate limiting** (per-user or global).
    

---

If you want, I can create:

➡️ **Round 4 Backend — Authentication + JWT**  
➡️ **Round 5 Backend — File Upload + Validation**  
➡️ **Round 6 Backend — Rate Limiting + Caching**  
➡️ **Round 7 Backend — Payment/Order System**  
➡️ **Round 8 Backend — Carts + Orders + Checkout**

Just tell me the next backend topic!