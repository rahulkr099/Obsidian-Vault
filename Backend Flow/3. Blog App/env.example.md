# 2️⃣ `.env.example` — Environment Variables

### Purpose

Stores configuration separately from code.

### Pseudocode

```
DEFINE server port
DEFINE database connection string
DEFINE JWT secret key
DEFINE JWT expiry duration
```

🗣

> “Environment variables keep secrets and configs out of source code.”