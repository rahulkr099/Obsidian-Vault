## 4. App Startup

```
START SERVER

Load environment variables

Connect to database

Register routes:
  /auth/register
  /auth/login
  /expenses (CRUD)
  /expenses/analytics
  /expenses/export

Start listening on PORT
```
