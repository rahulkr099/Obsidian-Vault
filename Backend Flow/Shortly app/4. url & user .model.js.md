Perfect, Rahul 👍  
Here is **ONE clean pseudocode snippet**, same style, no extra explanation.

---



---

### 🔹 `user.model.js` — PSEUDOCODE

```text
IMPORT database library

DEFINE User schema

    STORE first name
        - required
        - remove extra spaces

    STORE last name
        - required
        - remove extra spaces

    STORE email address
        - required
        - remove extra spaces

    STORE password
        - required only if auth type is NOT google

    STORE authentication type
        - allowed values: local, google
        - default: local

    STORE user role
        - allowed values: admin, user

    STORE reset password token

    STORE access token

    STORE refresh token

    STORE reset password expiry time

CREATE User model from schema

EXPORT User model
```

---

This is **short, clean, and easy to revise**.

If you want next:

- controller pseudocode
    
- password reset flow pseudocode
    
- JWT flow pseudocode
    

Just say 👍