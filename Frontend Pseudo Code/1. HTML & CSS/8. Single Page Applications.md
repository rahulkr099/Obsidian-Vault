## ✅ Improved SPA Code (Simple & Clean)

### ✔ What this version fixes

- No inline `onclick`
    
- No `innerHTML` for user-like content
    
- Central routing logic
    
- Easy to add more pages later
    

---

### 📄 **Better SPA Example (Pure JS)**

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Simple SPA</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 1rem;
        }

        nav a {
            margin-right: 1rem;
            cursor: pointer;
            color: blue;
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <nav>
        <a data-page="home">Home</a>
        <a data-page="about">About</a>
    </nav>

    <div id="content"></div>

    <script>
        const content = document.getElementById("content");

        function renderPage(page) {
            content.textContent = ""; // clear content

            const heading = document.createElement("h2");

            if (page === "home") {
                heading.textContent = "Home";
            } else if (page === "about") {
                heading.textContent = "About";
            } else {
                heading.textContent = "Page Not Found";
            }

            content.appendChild(heading);
        }

        document.querySelector("nav").addEventListener("click", function (event) {
            if (event.target.dataset.page) {
                renderPage(event.target.dataset.page);
            }
        });

        // Load default page
        renderPage("home");
    </script>

</body>
</html>
```

---

## 🧠 Why this is better (simple words)

|Old Code|New Code|
|---|---|
|Inline `onclick`|Event listener|
|`innerHTML`|`createElement + textContent`|
|Hard to extend|Easy to add pages|
|Not safe|Safe & clean|

This is how **real SPA logic starts** before React/Vue.

---

## 🚀 Easy next upgrades (try later)

1️⃣ Add browser history (`pushState`)  
2️⃣ Add active link highlight  
3️⃣ Load content from object map  
4️⃣ Add 404 page  
5️⃣ Convert this to **React router mental model**

---

## 🎯 Interview explanation (short)

> This is a basic SPA where navigation updates the content dynamically without reloading the page.  
> I use event delegation and a render function to control page content.

---

If you want next:

- Pseudocode for this SPA
    
- SPA with URL change
    
- Mini router implementation
    
- Same app in React (very simple)
    

Just tell me 👍

```
START

1. FIND the navigation bar element
2. FIND the content display element

--------------------------------------------------

3. DEFINE function renderPage(pageName)

   a. CLEAR the content area

   b. CREATE a heading element

   c. IF pageName is "home"
         SET heading text to "Home"

      ELSE IF pageName is "about"
         SET heading text to "About"

      ELSE
         SET heading text to "Page Not Found"

   d. ADD the heading element to the content area

--------------------------------------------------

4. ADD a click event listener to the navigation bar

   a. WHEN a navigation link is clicked
      i.   CHECK if clicked element has a page name
      ii.  CALL renderPage with that page name

--------------------------------------------------

5. LOAD the default page
   → CALL renderPage("home")

END


```